import { createTheme } from "@mui/material";

const OrangeTheme = createTheme({
    palette: {
      primary: {
        main: '#E3472F', // Replace with your desired color
      },
    },
  });
  

export default OrangeTheme;
